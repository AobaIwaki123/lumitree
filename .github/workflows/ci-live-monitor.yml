name: CI - Live API Monitoring

on:
  schedule:
    # Runs daily at 00:00 UTC (09:00 JST)
    - cron: '0 0 * * *'
  push:
    branches:
      - release
  workflow_dispatch:

jobs:
  live-api-check:
    name: Test Live TimeTree API
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Setup Go
        uses: actions/setup-go@v7
        with:
          go-version: '1.23'
          check-latest: true
          cache: false

      - name: Build lumitree CLI
        run: go build -o lumitree ./cmd/lumitree

      - name: Test live TimeTree public calendar fetch with retry
        run: |
          echo "Checking live TimeTree API connectivity..."
          MAX_RETRIES=30
          RETRY_DELAY=10
          SUCCESS=false

          for i in $(seq 1 $MAX_RETRIES); do
            echo "Attempt $i of $MAX_RETRIES: Fetching public events..."
            if ./lumitree get ilife_official --json > /tmp/live_events.json 2>&1; then
              COUNT=$(grep -o '"title":' /tmp/live_events.json | wc -l || echo 0)
              if [ "$COUNT" -gt 0 ]; then
                echo "Successfully fetched $COUNT events from live TimeTree."
                SUCCESS=true
                break
              fi
            fi
            echo "Fetch attempt $i failed. Retrying in ${RETRY_DELAY}s..."
            sleep "$RETRY_DELAY"
          done

          if [ "$SUCCESS" != "true" ]; then
            echo "::error::Failed to fetch live events from TimeTree after $MAX_RETRIES attempts!"
            exit 1
          fi

  ical-export-check:
    name: Verify iCalendar Export
    needs: [live-api-check]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Setup Go
        uses: actions/setup-go@v7
        with:
          go-version: '1.23'
          check-latest: true
          cache: false

      - name: Build lumitree CLI
        run: go build -o lumitree ./cmd/lumitree

      - name: Test live iCalendar generation
        run: |
          echo "Testing live iCalendar generation..."
          ./lumitree ics ilife_official
          if [ ! -f "ilife_official.ics" ]; then
            echo "::error::ilife_official.ics file was not generated!"
            exit 1
          fi
          if ! grep -q "BEGIN:VCALENDAR" ilife_official.ics; then
            echo "::error::iCalendar format validation failed!"
            exit 1
          fi
          echo "iCalendar generation verified successfully."
          rm -f ilife_official.ics
